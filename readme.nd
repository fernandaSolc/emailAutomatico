# Enviador de e-mail usando Python e Outlook

Este script em Python utiliza o módulo `win32com` para interagir com o aplicativo Outlook e enviar um e-mail.

## Como usar
1. Certifique-se de ter o módulo `pywin32` instalado.
2. Substitua o endereço de e-mail no campo `email.To` pelo destinatário desejado.
3. Execute o script e verifique a saída no console para confirmar que o e-mail foi enviado.

---
# Email sender using Python and Outlook

This Python script uses the win32com module to interact with the Outlook application and send an email.

## How to use
1. Make sure to have the pywin32 module installed.
2. Replace the email address in the email.To field with the desired recipient.
3. Run the script and check the console output to confirm that the email was sent.